# brau assets

Drop input images for image-to-video here.

For the `animate-hero` brief, save the spa-chair photo as:

    companies/brau/assets/hero.jpg

The brief references it via `image: assets/hero.jpg`. Veo uses it as the
starting frame and animates it. Replace it (or add more `image:` deliverables)
to animate other shots.
