"""Pluggable content-generation providers.

Each provider implements `ContentProvider` and is looked up by name through the
registry. Add a new provider by creating a module here and registering it in
`registry.py`.
"""
